#!/bin/bash

# 生产环境专用 Composer

COMPOSE_PATH=$1
CMD=$2
docker run --rm \
       -v $PWD/tmp/cache:/tmp/cache \
       -v $PWD/app/${COMPOSE_PATH}:/app \
       khs1994/php-fpm \
       composer ${CMD}
