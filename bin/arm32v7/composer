#!/bin/bash
echo -e "$0\n"
COMPOSE_PATH=$1
CMD=$2
TAG=7.1.9

docker run --rm \
       -v $PWD/tmp/cache:/tmp/cache \
       -v $PWD/app/${COMPOSE_PATH}:/app \
       khs1994/arm32v7-php-fpm:${TAG} \
       composer ${CMD}
