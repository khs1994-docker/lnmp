#!/bin/bash

COMPOSE_PATH=$1
CMD=$2
TAG=7.1.9

docker run --rm \
       -v $PWD/tmp/cache:/tmp/cache \
       -v $PWD/app/${COMPOSE_PATH}:/app \
       lnmp-arm64v8-php:${TAG} \
       composer ${CMD}
