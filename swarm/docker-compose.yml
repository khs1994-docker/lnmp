version: "3.4"
services:
  php7:
    build:
      context: ../app/demo
      dockerfile: Dockerfile
    image: ${PHP_DOCKER_IMAGE}

  nginx:
    build:
      context: ../config/nginx
      dockerfile: Dockerfile
    image: ${NGINX_DOCKER_IMAGE}
    ports:
      - "80:80"
    depends_on:
      - php7
