version: '3'

services:
  laravel:
    build:
      context: ./dockerfile/laravel/
      dockerfile: Dockerfile
    image: lnmp-laravel:latest
    volumes:
      - ./app:/app
      - ./var/lib/laravel:/tmp/cache
    env_file: .env
    command: laravel new $LARAVEL_APP_NAME
    tty: true
