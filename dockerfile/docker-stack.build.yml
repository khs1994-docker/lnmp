version: "3.4"
services:

  # 先构建镜像，再部署集群
  php7:
    build:
      context: swarm/php-fpm/
      dockerfile: Dockerfile
    image: khs1994/php-fpm:swarm-${KHS1994_LNMP_PHP_VERSION}-alpine3.4

  nginx:
    build:
      context: swarm/nginx/
    image: khs1994/nginx:swarm-${KHS1994_LNMP_NGINX_VERSION}-alpine
