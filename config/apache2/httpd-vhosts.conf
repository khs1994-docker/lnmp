# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#

Listen 443

SSLCipherSuite HIGH:MEDIUM:!MD5:!RC4:!3DES
SSLProxyCipherSuite HIGH:MEDIUM:!MD5:!RC4:!3DES

SSLHonorCipherOrder on

SSLProtocol all -SSLv3
SSLProxyProtocol all -SSLv3

SSLSessionCache        "shmcb:/usr/local/apache2/logs/ssl_scache(512000)"
SSLSessionCacheTimeout  300

<VirtualHost *:443>
    ServerAdmin khs1994@khs1994.com
    DocumentRoot "/app/test"
    ServerName demo.lnmp.khs1994.com
    ServerAlias demo.lnmp.khs1994.com

    ErrorLog "logs/error_log"
    CustomLog "logs/access_log" common

    SSLEngine on

    <FilesMatch \.php$>
        SetHandler "proxy:fcgi://php7:9000"
    </FilesMatch>

    SSLCertificateFile conf/ssl/demo.lnmp.khs1994.com.crt
    SSLCertificateKeyFile conf/ssl/demo.lnmp.khs1994.com.key

    <Directory "/app/test" >
      Options Indexes FollowSymLinks
      AllowOverride None
      Require all granted
    </Directory>

</VirtualHost>

<VirtualHost _default_:80>
    ServerAdmin khs1994@khs1994.com
    DocumentRoot "/app/demo"
    ServerName 127.0.0.1

    ErrorLog "logs/error_log"
    CustomLog "logs/access_log" common

    <FilesMatch \.php$>
        SetHandler "proxy:fcgi://php7:9000"
    </FilesMatch>

    <Directory "/app/demo" >
      Options Indexes FollowSymLinks
      AllowOverride None
      Require all granted
    </Directory>
</VirtualHost>
