# [CFSSL](https://github.com/cloudflare/cfssl)

```bash
$ sudo curl -L https://github.com/cloudflare/cfssl/releases/download/1.5.0/cfssl_linux-amd64 -o /usr/local/bin/cfssl

$ sudo curl -L https://github.com/cloudflare/cfssl/releases/download/1.5.0/cfssl-certinfo_linux-amd64 -o /usr/local/bin/cfssl-certifo

$ sudo curl -L https://github.com/cloudflare/cfssl/releases/download/1.5.0/cfssljson_linux-amd64 -o /usr/local/bin/cfssljson
```

```bash
├── cfssl
├── cfssl-bundle
├── cfssl-certinfo
├── cfssl-newkey
├── cfssl-scan
├── cfssljson
├── mkbundle
└── multirootca
```
