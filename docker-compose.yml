version: "3.3"
services:

  mysql:
    restart: always
    # 开发环境使用自定义镜像，构建规则在 override 中定义
    # 变量从 .env 文件读取例如镜像 tag
    image: "mysql:${KHS1994_LNMP_MYSQL_VERSION}"
    env_file:
      - ./.env
    #environment:
    #  # your password
    #  - MYSQL_ROOT_PASSWORD=mytest
    networks:
      - backend
    volumes:
      - mysql-data:/var/lib/mysql:rw
      - ./logs/mysql:/var/log/mysql:rw
      - ./backup/mysql:/backup:rw
    command: ["mysqld","--character-set-server=utf8mb4"]

  redis:
    restart: always
    image: "lnmp-redis:${KHS1994_LNMP_REDIS_VERSION}-alpine"
    env_file: .env
    # ports:
    #   - "6379"
    networks:
      - backend
    volumes:
      - ./config/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis-data:/data:rw
      - ./logs/redis:/var/log/redis:rw
    command: ["redis-server","/usr/local/etc/redis/redis.conf"]

  # memcached:
  #   restart: always
  #   image: lnmp-memcached:${KHS1994_LNMP_MEMCACHED_VERSION}-alpine
  #   networks:
  #     - backend
  #   # ports:
  #   #   - "11211"
  #
  # rabbitmq:
  #   restart: always
  #   image: lnmp-rabbitmq:${KHS1994_LNMP_RABBITMQ_VERSION}-management-alpine
  #   volumes:
  #     - rabbitmq-data:/var/lib/rabbitmq:rw
  #   networks:
  #     - backend
  #   # ports:
  #   #   - "15672"
  #   #   - "5672"
  #
  # postgresql:
  #    restart: always
  #   image: lnmp-postgres:${KHS1994_LNMP_POSTGRESQL_VERSION}-alpine
  #   env_file: .env
  #   volumes:
  #     - postgresql-data:/var/lib/postgresql/data:rw
  #   networks:
  #     - backend
  #   # ports:
  #   #   - "5432"
  #
  # mongo:
  #   restart: always
  #   image: mongo:${KHS1994_LNMP_MONGODB_VERSION}
  #   env_file:
  #     - ./.env
  #   volumes:
  #     - ./config/mongodb/mongod.conf:/etc/mongod.conf:ro
  #     - mongodb-data:/data/db:rw
  #     - ./logs/mongodb/mongo.log:/var/log/mongodb/mongo.log:rw
  #   networks:
  #     - backend
  #   # ports:
  #   #   - "27017"
  #   command: ["mongod","--config","/etc/mongod.conf"]
  #
  # gogs:
  #   image: gogs/gogs:${KHS1994_LNMP_GOGS_VERSION}
  #   ports:
  #     - "22:22"
  #   networks:
  #     - frontend
  #     - backend
  #   volumes:
  #     - gogs-data:/data:rw
  #     - ./config/gogs/app.prod.ini:/data/gogs/conf/app.ini:ro
  #     - ./config/nginx/ssl:/data/ssl:ro

  php7:
    restart: always
    image: "lnmp-php:${KHS1994_LNMP_PHP_VERSION}-alpine"
    # container_name: khs1994-docker-lnmp-php-fpm
    volumes:
      - ./app:/app:rw
      - ./config/php/php-development.ini:/usr/local/etc/php/php.ini:ro
      - ./config/php/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini:ro
      - ./config/php/php-fpm.d/docker.conf:/usr/local/etc/php-fpm.d/docker.conf:ro
      - ./config/php/php-fpm.d/www.conf:/usr/local/etc/php-fpm.d/www.conf:ro
      - ./logs/php-fpm:/var/log/php-fpm:rw
    networks:
      - frontend
      - backend
    dns:
      - 114.114.114.114
    command: ["php-fpm","-R"]
    environment:
      - TZ=Asia/Shanghai
    # entrypoint: /code/entrypoint.sh
    # entrypoint:
    # - php
    # - -d
    # - zend_extension=/usr/local/lib/php/extensions/no-debug-non-zts-20100525/xdebug.so
    # - -d
    # - memory_limit=-1
    # - vendor/bin/phpunit
    # extra_hosts:
    #   - "somehost:162.242.195.82"
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost"]
    #   interval: 1m30s
    #   timeout: 10s
    #   retries: 3
    # ports:
    #   - "3000-3005"
    #   - "9090-9091:8080-8081"
    #   - "127.0.0.1:8001:8001"
    #   - "127.0.0.1:5000-5010:5000-5010"
    #   - "6060:6060/udp"

  nginx:
    restart: always
    image: "lnmp-nginx:${KHS1994_LNMP_NGINX_VERSION}-alpine"
    env_file: .env
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./app:/app
      - ./config/nginx:/etc/nginx/conf.d:ro
      - ./config/etc/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./logs/nginx:/var/log/nginx:rw
    dns: 8.8.8.8
    networks:
      - frontend

networks:
  frontend:
  backend:

volumes:
  mysql-data:
  # pg-data:
  # mongodb-data:
  redis-data:
  # rabbitmq-data:
  # postgresql-data:
  # gogs-data:
